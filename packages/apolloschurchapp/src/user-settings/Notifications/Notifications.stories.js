import React from 'react';
import { storiesOf } from '@apollosproject/ui-storybook';

import Notifications from './Notifications';

storiesOf('user-settings/Notifications', module).add('default', () => (
  <Notifications />
));
